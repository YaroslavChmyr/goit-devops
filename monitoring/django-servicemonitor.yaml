apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: django-app-monitor
  namespace: monitoring
  labels:
    app: django-app
spec:
  selector:
    matchLabels:
      app: django-app
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
